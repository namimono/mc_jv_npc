# Jev NPC Demo · 开发进度

更新日期：2026-09-25。对应方案：[design.md](design.md)、[layered-agent.md](layered-agent.md)。使用说明：[README](../README.md)。平台：Fabric / Minecraft 1.21.1 / Java 21。

**当前实现分支：`codex/jev-led-harness`。** 先提交方案，再改为 Jev 统一高层决策、DeepSeek 按需承担玩家交流。新目标支持方法组合、多阶段持久进度与 1–256 块的阶段采集量；不再有 DeepSeek 直接开工、无 Jev 本地语义回退或关键词直接授权。78 项单测、36 项 GameTest、构建及真实客户端通过；十二块圆石交付、受伤治疗和回家由 Jev 完成。执行中改为累计八块时，Jev 选择暂停交流，保留任务 ID 及已采三块，再采五块、准确交付八块并回家；两个任务的身体执行期间新增 DeepSeek 调用均为零。修订保留阶段证据、累计交付量和余料，暂停期间权限仍按时到期，紧急避险不受阻碍。当前方案见 [Harness 方案](deepseek-harness-proposal.md)，最新验证证据见 [客户端验收](client-validation.md)。

**历史基线（分支 `feat/layered-agent`）：分层 Agent 的五步已全部实现，并通过真实客户端验收。** 所有移动改走新的寻路器（跳、落、游、跨缺口、挖穿、垫高、搭桥、避岩浆）；缺垫脚方块时代码先就近挖再继续；冒险或挖人造方块前在聊天框问主人，主人直接回话即可；NPC 会主动提醒天黑、受伤、背包满、工具快坏、看到钻石；空闲时按自己的需求和性格找事做；配置 DeepSeek 后可以自由对话，并能把对话变成经过校验的任务。66 项 JUnit、31 项 GameTest、构建通过；真实客户端使用官网 Jev 和 DeepSeek 完成闲聊、搭桥、两种追问路由、提问授权、自主补充方块和天黑提醒，留存六张截图，详见 [客户端验收记录](client-validation.md)。

| 新交付项 | 状态 | 说明 |
| --- | --- | --- |
| 分层设计文档 | 已完成 | [layered-agent.md](layered-agent.md) |
| Navigator 寻路器 | 已验证 | 纯 Java 规划器 12 项单测；8 项地形 GameTest；真实客户端搭桥 |
| 恢复规则 | 已验证 | 缺方块先挖再搭桥（GameTest 与真实客户端）；不挖脚下与悬空方块；不进交付账本 |
| 发言出口与提问 | 已验证 | 去重、冷却、超时默认、否定词优先单测；GameTest 与真实客户端提问 → 聊天答复 → 授权挖穿 |
| 主动观察发言 | 部分验证 | 天黑提醒经真实客户端验证；受伤、背包满、工具快坏、钻石提醒只有代码路径，未单独验收 |
| 自主需求 | 已验证 | 需求与性格权重单测；无 key 本地选择（GameTest）；真实 Jev 选择 stock_blocks（客户端） |
| 空闲小动作 | 已验证 | 捡非玩家扔出的掉落物（GameTest）；看向主人未单独断言 |
| DeepSeek 对话 | 已验证 | HTTP／对话单测 11 项；真实客户端五次对话涵盖纯聊天、合法意图和授权追问 |

**已知限制（新增部分）。** 寻路不开门、不爬梯子藤蔓、不下潜，单次搜索限起点 64 格；跑酷只跨 1 格缺口。跟随或守卫时遇到需要授权的路线不会提问，只会隔几秒重试。只有 DeepSeek 没有 Jev key 时，已知意图按固定顺序推进；攻击因无法可靠选择指定生物而拒绝。追问保留授权问题，任务使用带上下文的复述；近期发言和上次任务结果进入对话，历史助手回复保持 JSON 格式。DeepSeek 回复内容和是否转成任务存在模型差异；验收只覆盖少量样本。

**以下为 2026-09-23 的历史状态。**

**多轮循环已补上真实树冠遮挡和缺省数量回归修复。** 在真实 Jev 隔离客户端中，四格云杉树干完全被低垂树叶包住时可先清除挡路树叶，再采集交付四块原木（5 轮）；带用户报告中的旧失败记忆执行“挖地面”成功（5 轮）。27 项单测、17 项 GameTest、构建通过。详情见 [客户端验收记录](client-validation.md)。之前简单树形的验收没有覆盖密集树冠，新增失败测试已锁定此缺口。

| 交付项 | 状态 | 说明 |
| --- | --- | --- |
| 项目初始化、版本锁定、Gradle wrapper | 已完成 | Fabric Loader 0.19.5、Fabric API 0.116.17+1.21.1、Loom 1.16.3 |
| 方案与原始调研落盘 | 已完成 | `docs/design.md`、`docs/research.md` |
| 自定义人形 NPC、名称、装备显示 | 已验证 | 真实客户端截图确认人物、名称、手持物品及铁胸甲可见 |
| 本地移动、跟随、等待、守卫、攻击、撤退 | 代码已实现，部分验证 | 移动到达 GameTest 已通过；战斗和复杂地形体验待后续测试 |
| 有限砍树、挖石头、3×3 平台、装备、进食 | 代码已实现，核心自动检查已通过 | 真正修改世界、计算掉落与消耗材料；边界见方案 |
| 行为打断、一级暂停与恢复 | 代码已实现，部分验证 | 暂停任务存档恢复已检查；完整危险触发场景待用户安排 |
| 定向聊天、受击、天气、任务结果与低频事件 | 代码已实现，待完整验证 | 合并、冷却、单在途和服务器请求预算有单元测试 |
| 官网 Jev HTTP 接入 | 已通过官网验收 | 真实中文指令正确选择 follow_owner、wait_here、equip_armor、build_platform 并执行 |
| key 配置与热加载 | 代码已实现 | 行为参数在可提交的 `jev-npc.json`；key 只在 `jev-npc.secret.json`，文件名被忽略，权限尽量为 600；日志不打印 key |
| 角色、主人、背包、任务和短记忆持久化 | 代码已实现，核心自动检查已通过 | 手动控制状态也持久化；不持久化在途网络请求 |
| 模组打包 | 已完成 | `build/libs/jev-npc-0.1.0.jar` |

**模块与职责。**

| 代码 | 职责 |
| --- | --- |
| `JevNpcMod` | 注册实体、属性、事件、配置和服务器生命周期 |
| `entity/JevNpcEntity` | 实体、主人、背包、记忆、存档和赠送交互 |
| `behavior/ActionPlan`、`Skill` | 已绑定参数的有限动作 |
| `behavior/SkillRunner` | 持续执行、寻路、方块工作、装备、暂停恢复与本地避险 |
| `ai/NpcBrain` | 保留目标、调度解释／工具决策、应用反馈、取消与循环预算 |
| `ai/AgentTask`、`GoalIntent` | 类型化目标、进度、交付账本、有限历史、存档 |
| `ai/EnvironmentTools` | 有界真实环境观察、候选绑定、已失败或消失目标排除 |
| `ai/JevClient` | 官网请求、响应校验、异步超时、错误脱敏 |
| `ai/DecisionGate`、`EventInbox`、`RequestBudget` | 旧结果隔离、事件合并、全服调用预算 |
| `config/` | 配置载入、默认值、参数范围和 key 来源 |
| `command/NpcCommands` | 管理命令、无 key 本地技能和定向自然语言入口 |
| `src/client/.../JevNpcClient` | 客户端人形、手持物品与盔甲渲染 |

**早期验证记录（最新结果见客户端验收记录）。**

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `./scripts/dev.sh test runGameTest build --console=plain` | 通过 | 最后一次运行 `BUILD SUCCESSFUL`，2026-09-22 16:03 左右 |
| JUnit：决策调度 | 6 项通过 | `build/test-results/test/TEST-dev.jev.npc.ai.DecisionSchedulingTest.xml` |
| JUnit：HTTP 接入与错误处理 | 8 项通过 | `build/test-results/test/TEST-dev.jev.npc.ai.JevClientTest.xml` |
| Minecraft GameTest | 6 项通过 | `work/verification.log` 中 `All 6 required tests passed` |
| 客户端启动 | 启动和资源加载完成并正常退出 | `run/logs/`；界面工具无法选中 Java 游戏窗口，未完成目视体验检查 |
| 官网 key / 实际推理 | 未执行 | 配置检查时 key 为空；不推断模型可用或准确率 |

GameTest 最新增：不可接近原木跳过、实际入水、本任务物品交付与存档、新指令替换旧目标、循环预算、观察候选过滤、允许主人指定铁傀儡但禁止攻击玩家。原有覆盖：实际导航到达；原木采集与掉落；石头掉圆石及工具耐久消耗；9 块木板的平台建造；建筑不覆盖已有方块；装备、面包消耗及主人／背包／暂停任务存档恢复。

实现期间已修正：1.21.1 的建造能力检查使用 `owner.getAbilities().mayBuild`；重复选择相同动作不重置进度；聊天前缀按固定长度剥离；取消后旧响应不能解除手动控制；畸形 key 返回错误状态而不在游戏线程抛出异常；NPC 死亡时背包按掉落流程处理。

**已知限制与待办。**

- 历史四条基础指令和新增三条多轮指令通过官网验证；指定铁傀儡攻击与禁止攻击玩家通过 GameTest。完整战斗打断、多人场景及中文表达覆盖率仍待扩展验证。
- 任意建筑、自由对话、任意数量／装备参数和通用开放式规划仍不在范围；现已支持有限工具内的多轮观察、执行、失败恢复与交付；聊天为模板，建造仅支持橡木平台。
- 地形较复杂时可能因路径不可达而失败；不挖通路、不搭脚手架、不跨维度跟随主人。
- 树木由附近树叶启发式判断，无法保证区分所有玩家木建筑；未接入第三方领地保护模组。
- 只支持一级暂停任务和最近 8 条事件；不具有完整关系数值、长期记忆检索或自主学习。
- 自定义实体没有跨 Minecraft 版本的数据迁移规则，启动会出现 `No data fixer registered` 日志；Demo 固定为 1.21.1。
- GameTest 运行在独立测试环境，不会调用付费模型，也不能证明生产服务器的延迟、吞吐或配额。

代码、配置模板、文档和测试源码均已保存在项目目录；构建缓存、开发存档、实际 key 配置和日志未纳入版本控制。
